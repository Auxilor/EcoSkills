stats:
  - id: saturation # The ID of the stat, to be used in skills
    name: "&#f5aa42🍖 Saturation" # The name of the stat, shown to players
    max-level: 200 # The max obtainable level of the stat - this is required in order to generate the correct number of levels

    effects:
      - id: hunger_multplier
        args:
          multiplier: "1 - (%level% / 300)"

    conditions: [ ]
